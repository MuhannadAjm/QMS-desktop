{
  "$schema": "https://schemas.tauri.app/v2/capabilities.json",
  "identifier": "default",
  "description": "Default permissions for QMS Desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
